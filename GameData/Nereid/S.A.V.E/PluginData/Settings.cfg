S.A.V.E
{
	// WindowPos can have one of the following values:
	//
	//	upperRight      Window is in the upper right corner
	//	upperLeft       Window is in the upper left corner
	//	floating        Window is floating
	//
	// If none of the above are specified, then the window will behave as originally

	WindowPos = floating

	// List directories that should NOT be backed up here
	Exclude = KaptainsLog
}
